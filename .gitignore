# Per-machine runtime + usage history — never commit (it's your personal usage data)
.state/

# Python
__pycache__/
*.pyc
*.pyo
.ruff_cache/

# Claude Code workspace
.claude/

# Rust build output (Cargo.lock IS committed — this is a binary)
/target/

# OS cruft
.DS_Store
